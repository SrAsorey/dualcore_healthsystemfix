Config = {
    --? Debug
    debug = true,
    debugThreadSpam = true, -- This will print spam every time when the health is saved

    --? Health verifications
    hasToWait = false, -- If the script has to wait more or less to player spawn
    waitTime = 3000,

    --? Hud settings
    useDualcoreHud = true,

    useHudUpdate = false, -- Open custom_client.lua and put your things to do <3
}
